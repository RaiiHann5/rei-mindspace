rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /logos/{userId}/{fileName} {
      allow read: if true; // logos/QRIS images are shown on public invoice pages
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
